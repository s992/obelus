apiVersion: v1
kind: Service
metadata:
  name: {{ include "obelus.fullname" . }}
  labels:
    {{- include "obelus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "obelus.selectorLabels" . | nindent 4 }}
