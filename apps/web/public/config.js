window.__OBELUS_CONFIG__ = {
  // Optional runtime override for API base URL (for container/helm deployments).
  // Leave empty to use Vite env fallback and, if unset, host-based :4000 fallback.
  API_BASE_URL: "",
};
